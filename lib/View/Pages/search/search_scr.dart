import 'package:flutter/material.dart';

class SearchScr extends StatelessWidget {
  const SearchScr({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("SearchScr");
  }
}
